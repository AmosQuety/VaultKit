{
  "name": "vaultkit",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^1.6.0",
    "typescript": "^5.0.0",
    "pnpm": "^8.0.0"
  }
  ,"pnpm.overrides": {
    "zod": "4.4.3"
  }
}
