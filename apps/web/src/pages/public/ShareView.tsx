import { ApprovalCard } from '../../components/share/ApprovalCard';

export function ShareViewPage() {
  return (
    <div style={{ minHeight: '100vh', padding: '1rem', display: 'grid', alignItems: 'center' }}>
      <ApprovalCard />
    </div>
  );
}